<nav class="site-nav">
  <a href="{{@site.url}}" class="site-nav-logo">
    {{#if @site.logo}}
      <img src="{{@site.logo}}" alt="{{@site.title}}">
    {{else}}
      <span>ThreatForged_AI</span>
    {{/if}}
  </a>
  <ul class="nav-links">
    {{#foreach @site.navigation}}
      <li><a href="{{url}}">{{label}}</a></li>
    {{/foreach}}
  </ul>
  <a href="https://threatforged.ai/#contact" class="nav-cta">Book a Demo</a>
  <button class="hamburger" id="hamburger" aria-label="Menu">
    <span></span><span></span><span></span>
  </button>
</nav>
<div class="mobile-menu" id="mobile-menu">
  {{#foreach @site.navigation}}
    <a href="{{url}}">{{label}}</a>
  {{/foreach}}
  <a href="https://threatforged.ai/#contact" class="mobile-cta">Book a Demo</a>
</div>
